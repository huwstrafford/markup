enum Endpoint {
    Home = "/",
    Documentation = "/docs",
    Repository = "https://github.com/samueldobbie/markup",
    DataGenerator = "/generator",
    Annotate = "/annotate",
}

export default Endpoint
